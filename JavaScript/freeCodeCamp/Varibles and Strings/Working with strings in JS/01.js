// Bracket notation and accessing character from the string

// string is sequence of character and character can be rerieveed and it's position is known as index
// index is zero based

let name = "aman";
console.log(name[0]);

// length of the string
// str.length -> gives the length like for aman 4
// to get last char of the string
console.log(name[name.length-1]);
